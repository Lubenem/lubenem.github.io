## Packages
aos | Scroll animations for sections
date-fns | Date manipulation for the booking calendar
react-day-picker | Calendar component for booking wizard
framer-motion | Smooth transitions for the booking wizard steps

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
Client-side only booking flow as requested (no backend persistence).
